{
  "version": 2,
  "builds": [
    { "src": "server/**/*.js", "use": "@vercel/node" },
    { "src": "server/build/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/$1" },
    { "src": "/(.*)", "dest": "server/build/index.html" }
  ], 
  "scripts": 
    { "build": "npm run build" }
}